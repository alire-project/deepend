project Test_Dynamic_Pools_Linux is

   for Object_Dir use "./obj";
   for Exec_Dir use ".";
   for Main use ("test_dynamic_pools.adb");

   package Builder is
      for Default_Switches ("ada") use ("-j4", "-g");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-gnatVa", "-gnatf", "-g", "-gnata", "-gnat05", "-gnatwc.cdfjklm.op.pr.rtu.wx.x", "-gnatyaAbcdefhiIklmprStux");
   end Compiler;

   package Linker is
   end Linker;

   for Source_Dirs use (".", "../");

end Test_Dynamic_Pools_Linux;

